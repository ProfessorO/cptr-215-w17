#include <iostream>
using namespace std;

int main() {
	int count;
	for (count = 10; count >= 1; count--) {
		cout << count << endl;
	}
	// cout << "We counted down to " << count << endl;
	cout << "Blast off!" << endl;
	return 0;
}